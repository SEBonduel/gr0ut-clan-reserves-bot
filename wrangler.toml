name = "gr0ut-clan-reserves"
main = "src/index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

# Stockage du token WG (créé via : wrangler kv namespace create TOKENS)
# Colle l'id renvoyé ci-dessous.
[[kv_namespaces]]
binding = "TOKENS"
id = "e8d3f6a1a0ab41bbbe46b8bd0ebe1cf7"

# 06:00 UTC : renouvellement des tokens WG. Toutes les 30 min le soir : détection
# des réserves qui redeviennent activables (notif Discord). Samedi 17:00 + 18:00
# UTC : rappel "Jeux de guerre" (le code ne poste qu'à 19h heure de Paris, ce qui
# gère automatiquement l'heure d'été/hiver).
[triggers]
crons = ["0 6 * * *", "*/30 16-22 * * *", "0 17 * * 6", "0 18 * * 6"]

# Variables NON secrètes. Les secrets se posent avec `wrangler secret put NOM` :
#   WG_APP_ID, DISCORD_PUBLIC_KEY, DISCORD_TOKEN, DISCORD_APP_ID,
#   OFFICER_ROLE_IDS (id séparés par des virgules), CLAN_ID, LOGIN_SECRET
[vars]
WG_REGION = "eu"
